You are testing the TUI flow orchestrator with the real Codex backend.

Task:
1. Create this directory if it does not exist:
   `{output_dir}`
2. Write this exact file:
   `{draft_file}`
3. The file must be Markdown and include:
   - title: `# Codex Real Flow Draft`
   - a short bullet list with exactly three bullets
   - a line saying `created_by: codex`

Keep the response short after the file is written.
