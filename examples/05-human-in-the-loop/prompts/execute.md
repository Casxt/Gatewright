You are continuing the same Codex thread that just wrote the plan at
`{plan_file}`. The user has reviewed the plan and authorized you to
proceed.

Task:
1. Re-read `{plan_file}` to refresh your context on what to build.
2. Implement the script at `{script_file}`. Constraints:
   - Single file, < 60 lines.
   - Standard library only (`argparse`, `csv`).
   - Match the plan's sections — same inputs, same error handling.
   - Include a module-level docstring summarizing usage.
3. Reply with one short line confirming the script path.

If during implementation you find an open question from the plan that
matters, address it with your best judgment and add a brief comment in
the script noting the choice.

Important: do NOT execute the script in this step — the verify step will
run it under user supervision.
